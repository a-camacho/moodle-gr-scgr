/**
 * @module gradebook_report_scgr/formcontrol
 */

define(['jquery'], function($) {
    /**
     * @alias module:format_oustudyplan/formcontrol
     */
    var t = {
        init : function() {

            // your codes

        }

        // Other functions and variables here, comma-separated.

    };

    return t;
});